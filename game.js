const game=document.querySelector('#game'),cat=document.querySelector('#cat'),ob=document.querySelector('#obstacle'),scoreEl=document.querySelector('#score'),bestEl=document.querySelector('#best'),start=document.querySelector('#start'),over=document.querySelector('#over'),final=document.querySelector('#final');let playing=false,last=0,y=0,v=0,x=0,score=0,best=+localStorage.catJumpBest||0;bestEl.textContent=best;function reset(){y=0;v=0;x=game.clientWidth+80;score=0;scoreEl.textContent=0;cat.style.transform='translateY(0)';ob.style.transform='translateX(0)'}function begin(){reset();playing=true;start.classList.add('hidden');over.classList.add('hidden');last=performance.now();requestAnimationFrame(loop)}function jump(){if(playing&&y<=1)v=.82}function loop(t){if(!playing)return;let dt=Math.min(t-last,32);last=t;v-=.0021*dt;y+=v*dt;if(y<0)y=0,v=0;x-=(.28+Math.min(score*.003,.22))*dt;if(x<-80){x=game.clientWidth+80+Math.random()*180;score++;scoreEl.textContent=score}cat.style.transform=`translateY(${-y}px)`;ob.style.transform=`translateX(${x-(game.clientWidth+80)}px)`;let a=cat.getBoundingClientRect(),b=ob.getBoundingClientRect(),p=12;if(!(a.right-p<b.left+p||a.left+p>b.right-p||a.bottom-p<b.top+p||a.top+p>b.bottom-p)){playing=false;final.textContent=score;over.classList.remove('hidden');if(score>best){best=score;localStorage.catJumpBest=best;bestEl.textContent=best}return}requestAnimationFrame(loop)}document.querySelector('#startBtn').onclick=begin;document.querySelector('#again').onclick=begin;document.querySelector('#jump').onclick=jump;document.onkeydown=e=>{if(e.code==='Space'||e.code==='ArrowUp'){e.preventDefault();jump()}};game.onpointerdown=e=>{if(e.target===game||e.target===cat||e.target===ob)jump()};
